# ── PMC Overwatch — Dependencies ───────────────────────────────────
# Requires Python 3.10+ — relaxed pins for cross-platform compatibility.
# Only direct dependencies are listed; transitive deps are managed by pip.

groq
pynput
opencv-python
twitchio<3.0.0
faster-whisper
sounddevice
soundfile
numpy
ollama
python-dotenv
Pillow
edge-tts
google-genai
mss
fastapi
uvicorn
noisereduce
scipy

# ── Neural VAD (Silero) ──
torch
torchaudio
silero-vad

# ── Local Neural TTS (Kokoro) ──
kokoro-onnx
onnxruntime
huggingface-hub
