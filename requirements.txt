# ── PMC Overwatch — Pinned Dependencies ────────────────────────────
# Windows PC with Python 3.14 — relaxed pins for compatibility.
# Only direct dependencies are listed; transitive deps are managed by pip.

groq
pynput
opencv-python
twitchio<3.0.0
faster-whisper
sounddevice
soundfile
numpy
ollama
kokoro-onnx
python-dotenv
customtkinter
Pillow
edge-tts
